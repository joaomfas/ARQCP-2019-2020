#include <stdio.h>
#include "mixed_sum.h"

  int main(void) {
    
    int a = 0 , b = -1, pos = 8;
    
    int res = mixed_sum(a, b, pos);
    
    printf("resultado = %d\n", res);
     
	return 0; 
}
